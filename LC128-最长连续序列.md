### 思路

**问题描述**：给定一个未排序的整数数组 `nums`，找出数字连续的最长序列的长度。序列中的数字连续，但不要求在原数组中的位置连续。

**解题思路**：
1. 使用哈希集合（unordered_set）存储所有数字，以便快速查找。
2. 遍历数组中的每个数字 `x`：
   - 如果 `x-1` 不在集合中，说明 `x` 是某个连续序列的开始。
   - 从 `x` 开始，向后查找连续的数字（`x+1`, `x+2`, ...），直到找不到为止，计算当前序列长度。
   - 更新最大长度 `max_len`。
3. 优化：如果当前最大长度的一半大于等于集合大小，可以提前停止遍历，因为不可能找到更长的序列。

**复杂度分析**：
- **时间复杂度**：O(n)，其中 n 是数组长度。虽然有嵌套循环，但每个数字最多被访问两次（一次在遍历时，一次在查找连续序列时）。
- **空间复杂度**：O(n)，用于存储哈希集合。


### 代码
```c++
class Solution {
public:
    int longestConsecutive(vector<int>& nums) {
        unordered_set<int> st(nums.begin(), nums.end());
        int max_len =0;
        for (int x: nums){
            if (st.contains(x-1)){
                continue;

            }
                int cur = x;
                int len =1;

                while (st.find(cur+1)!= st.end()){
                    cur++;
                    len++;


                }
                max_len = max(max_len,len);
                if (max_len*2 >= st.size()){
                    break;
                }

        }
        return max_len;
        
    }
};
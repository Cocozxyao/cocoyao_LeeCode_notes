### 思路
Kadane 算法（动态规划优化版）

核心思想：

到每个位置 i 为止，最大的子数组和要么是「从 i 重新开始」，要么是「前面的和 + nums[i]」。

### 代码

```c++
class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int ans = nums[0];
        int sum =0;

        for(int num:nums){
            sum = max(num,num+sum);

            ans = max(ans,sum);
        }
        return ans;
    }
};
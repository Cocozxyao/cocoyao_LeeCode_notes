# 力扣 Hot100 总体性刷题要点

LeetCode Hot100 覆盖了面试中最常见的数据结构与算法题型。刷这 100 道题的重点不只是“做完”，而是建立一套稳定的解题框架：看到题目能快速判断类型，知道常用套路，能写出正确、简洁、复杂度合适的代码。

## 一、刷题的核心目标

Hot100 的价值主要在于训练以下能力：

1. 识别题型  
   看到题目后，判断它属于数组、哈希、双指针、滑动窗口、二叉树、动态规划、回溯、图论等哪一类。

2. 掌握模板  
   很多题并不是完全从零思考，而是对经典模板的变形。

3. 理解复杂度  
   面试中不仅要写出代码，还要说明时间复杂度和空间复杂度。

4. 提升边界处理能力  
   空数组、单节点、重复元素、负数、越界、递归终止条件等，是高频出错点。

5. 形成复盘习惯  
   做完一道题后，要总结“这题为什么这样做”“下次如何识别”。

## 二、常见题型与解题要点

### 1. 数组与哈希表

代表题目：

- 两数之和
- 字母异位词分组
- 最长连续序列
- 缺失的第一个正数

核心思想：

数组题经常考察遍历、原地修改、计数、去重和索引映射。哈希表适合解决“快速查找是否存在”“统计频率”“建立映射关系”的问题。

常见套路：

- 需要快速判断某个值是否出现过：用 `Set`
- 需要记录某个值对应的位置：用 `Map`
- 需要统计字符或数字频率：用哈希表或数组计数
- 要求 O(n) 时间复杂度时，通常要避免双重循环

典型思路：

```text
遍历数组
如果当前元素能和之前的信息组成答案，就返回或更新答案
否则把当前元素存入哈希表
```

注意点：

- 哈希表中存的是“值”还是“下标”
- 是否需要处理重复元素
- 是否可以原地修改数组
- 题目是否要求保持原顺序

### 2. 双指针

代表题目：

- 移动零
- 盛最多水的容器
- 三数之和
- 接雨水
- 反转链表
- 回文链表

核心思想：

双指针通常用来减少不必要的枚举。两个指针可以同向移动，也可以相向移动。

常见类型：

1. 左右指针  
   常用于有序数组、回文判断、区间收缩。

2. 快慢指针  
   常用于链表、中点查找、环检测、原地数组处理。

3. 多指针去重  
   常见于三数之和、四数之和。

典型判断：

- 数组有序：优先想左右指针
- 链表找中点：快慢指针
- 原地删除或移动元素：快慢指针
- 区间面积、最大/最小值：尝试左右收缩

注意点：

- 指针移动条件必须明确
- 去重时要注意循环边界
- 三数之和这类题，排序后再双指针是关键

### 3. 滑动窗口

代表题目：

- 无重复字符的最长子串
- 找到字符串中所有字母异位词
- 最小覆盖子串
- 和为 K 的子数组

核心思想：

滑动窗口本质是维护一个连续区间，并在区间变化时更新答案。

常见场景：

- 子串
- 子数组
- 连续区间
- 最长/最短满足条件的问题

常用模板：

```text
left = 0
for right in range(n):
    将 nums[right] 加入窗口

    while 窗口不满足条件:
        移除 nums[left]
        left++

    更新答案
```

如果是“最小满足条件窗口”，通常是在满足条件时收缩左边界。

如果是“最长满足条件窗口”，通常是在不满足条件时收缩左边界。

注意点：

- 窗口内需要维护什么信息：频率、和、最大值、匹配数量等
- `left` 何时移动
- 答案是在窗口扩大时更新，还是收缩时更新
- 子数组和为 K 不一定能用滑动窗口，因为存在负数时窗口单调性被破坏，通常用前缀和 + 哈希表

### 4. 前缀和

代表题目：

- 和为 K 的子数组
- 除自身以外数组的乘积
- 最大子数组和
- 矩阵区域和

核心思想：

前缀和用于快速计算区间和。

基本关系：

```text
sum(left, right) = prefix[right + 1] - prefix[left]
```

常见变形：

- 一维前缀和
- 二维前缀和
- 前缀和 + 哈希表
- 前缀积或左右乘积

子数组和为 K 的关键公式：

```text
prefix[j] - prefix[i] = k
=> prefix[i] = prefix[j] - k
```

所以遍历时记录之前出现过的前缀和次数。

注意点：

- `prefix[0] = 0` 很重要
- 哈希表初始化 `{0: 1}`
- 前缀和适合处理区间问题
- 有负数时，滑动窗口不一定适用，前缀和更稳定

### 5. 链表

代表题目：

- 反转链表
- 两两交换链表中的节点
- 合并两个有序链表
- 环形链表
- 相交链表
- 删除链表的倒数第 N 个节点

核心思想：

链表题重点不是算法多难，而是指针操作不能乱。

常用技巧：

1. 虚拟头节点 `dummy`  
   适合处理头节点可能变化的情况。

2. 快慢指针  
   找中点、判断环、找倒数第 N 个节点。

3. 三指针反转  
   常用于反转链表。

反转链表模板：

```text
prev = null
cur = head

while cur != null:
    next = cur.next
    cur.next = prev
    prev = cur
    cur = next

return prev
```

注意点：

- 修改 `cur.next` 前先保存 `next`
- 删除节点时要找到待删除节点的前一个节点
- 涉及头节点变化时优先使用 dummy
- 链表题画图非常重要

### 6. 二叉树

代表题目：

- 二叉树的中序遍历
- 二叉树的最大深度
- 翻转二叉树
- 对称二叉树
- 二叉树的直径
- 二叉树的层序遍历
- 从前序与中序遍历序列构造二叉树
- 二叉树的最近公共祖先

核心思想：

二叉树题大多可以从递归入手。递归的本质是明确每个节点要做什么。

常见遍历：

```text
前序：根 左 右
中序：左 根 右
后序：左 右 根
层序：BFS
```

递归三要素：

1. 函数定义  
   这个函数返回什么？

2. 终止条件  
   空节点时返回什么？

3. 单层逻辑  
   当前节点如何利用左右子树的结果？

常见套路：

- 求深度：后序递归
- 判断是否平衡：后序递归
- 路径相关：DFS + 回溯
- 层序遍历：队列 BFS
- 最近公共祖先：递归返回目标节点

注意点：

- 分清“自顶向下”和“自底向上”
- 路径题要注意回溯
- 二叉搜索树利用中序有序特性
- 递归返回值要设计清楚

### 7. 回溯

代表题目：

- 全排列
- 子集
- 组合总和
- 括号生成
- 单词搜索

核心思想：

回溯就是暴力搜索所有可能，并在不符合条件时剪枝。

通用模板：

```text
backtrack(路径, 选择列表):
    if 满足结束条件:
        收集结果
        return

    for 选择 in 选择列表:
        做选择
        backtrack(路径, 新的选择列表)
        撤销选择
```

常见问题：

- 子集：每个元素选或不选
- 组合：不关心顺序
- 排列：关心顺序，需要 used 数组
- 棋盘搜索：DFS + visited
- 括号生成：控制左右括号数量

注意点：

- 是否允许重复使用元素
- 是否需要排序去重
- `startIndex` 用于避免重复组合
- 回溯后一定要恢复现场
- 剪枝可以大幅减少搜索量

### 8. 动态规划

代表题目：

- 爬楼梯
- 打家劫舍
- 最大子数组和
- 最长递增子序列
- 最长公共子序列
- 编辑距离
- 完全平方数
- 零钱兑换
- 单词拆分

核心思想：

动态规划适合解决具有“重叠子问题”和“最优子结构”的问题。

五步法：

1. 定义 `dp[i]` 的含义
2. 推导状态转移方程
3. 初始化
4. 确定遍历顺序
5. 返回结果

常见类型：

1. 一维 DP  
   如爬楼梯、打家劫舍、最大子数组和。

2. 二维 DP  
   如最长公共子序列、编辑距离。

3. 背包问题  
   如零钱兑换、完全平方数、分割等和子集。

4. 区间 DP  
   如回文子串、最长回文子序列。

经典例子：

```text
dp[i] = 以 i 结尾的最优解
dp[i] = 前 i 个元素范围内的最优解
dp[i][j] = 字符串 s 的前 i 位和 t 的前 j 位的状态
```

注意点：

- `dp[i]` 的定义必须非常清楚
- 初始化往往决定代码是否正确
- 背包问题要分清 0-1 背包和完全背包
- 一维压缩时，遍历顺序很关键

### 9. 栈与单调栈

代表题目：

- 有效的括号
- 最小栈
- 每日温度
- 柱状图中最大的矩形
- 接雨水

核心思想：

栈适合处理“最近的某个元素”。单调栈适合找左边或右边第一个更大/更小的元素。

常见场景：

- 括号匹配
- 表达式计算
- 递增/递减关系
- 下一个更大元素
- 面积问题

单调栈模板：

```text
for i in range(n):
    while stack 不为空 and 当前元素破坏单调性:
        top = stack.pop()
        计算 top 对应的答案

    stack.push(i)
```

注意点：

- 栈里通常存下标，而不是值
- 单调递增栈用于找更小或更大元素，具体看出栈条件
- 面积题要注意左右边界
- 有些题需要在数组末尾加哨兵元素

### 10. 堆

代表题目：

- 数组中的第 K 个最大元素
- 前 K 个高频元素
- 合并 K 个升序链表
- 数据流的中位数

核心思想：

堆适合动态维护最大值或最小值。

常见套路：

- 求前 K 大：小顶堆维护 K 个元素
- 求前 K 小：大顶堆维护 K 个元素
- 多路合并：小顶堆
- 数据流中位数：一个大顶堆 + 一个小顶堆

注意点：

- 语言默认堆通常是小顶堆
- 求第 K 大不一定要完全排序
- 堆的时间复杂度通常是 `O(n log k)`
- 数据流题要保持两个堆的大小平衡

### 11. 图论与 BFS/DFS

代表题目：

- 岛屿数量
- 腐烂的橘子
- 课程表
- 实现 Trie
- 单词搜索

核心思想：

图题的核心是建模。很多矩阵题本质也是图搜索。

常见类型：

1. DFS  
   适合连通块、路径搜索、递归遍历。

2. BFS  
   适合最短路径、层级扩散。

3. 拓扑排序  
   适合课程依赖、有向无环图判断。

4. 并查集  
   适合连通性问题。

BFS 模板：

```text
queue 初始化
visited 标记

while queue 不为空:
    当前层循环
    扩展相邻节点
```

DFS 模板：

```text
dfs(x, y):
    if 越界 or 不合法:
        return

    标记 visited

    for 四个方向:
        dfs(nx, ny)
```

注意点：

- 矩阵搜索一定要检查越界
- 是否需要 visited
- BFS 求最短路径时按层遍历
- 拓扑排序关注入度

## 三、刷题时的通用思考流程

拿到一道题，可以按下面顺序思考：

1. 题目要求什么？  
   是求数量、最大值、最小值、路径、判断是否存在，还是返回具体结果？

2. 数据规模多大？  
   数据规模决定复杂度。`n <= 100` 可以考虑 O(n²)，`n >= 10^5` 通常要 O(n log n) 或 O(n)。

3. 是否有明显关键词？

| 关键词 | 常见方法 |
| --- | --- |
| 连续子数组 | 滑动窗口、前缀和 |
| 有序数组 | 双指针、二分 |
| Top K | 堆、快速选择 |
| 所有方案 | 回溯 |
| 最优解 | 动态规划、贪心 |
| 最近/匹配 | 栈 |
| 连通块 | DFS/BFS/并查集 |
| 依赖关系 | 拓扑排序 |

4. 能否暴力？  
   先想暴力解法，再优化。暴力思路往往能帮助推导更优解。

5. 状态或数据结构是什么？  
   解题的关键通常是“维护什么信息”。

6. 边界条件有哪些？  
   空、1 个元素、重复、负数、越界、极值。

## 四、不同题型的识别关键词

| 关键词 | 常见方法 |
| --- | --- |
| 两数之和、是否存在 | 哈希表 |
| 连续子串、连续子数组 | 滑动窗口 / 前缀和 |
| 有序数组 | 双指针 / 二分 |
| 第 K 大、Top K | 堆 / 快速选择 |
| 所有组合、排列、路径 | 回溯 |
| 最大/最小方案数 | 动态规划 |
| 括号、最近更大元素 | 栈 / 单调栈 |
| 树的深度、路径 | DFS |
| 层序、最短扩散 | BFS |
| 依赖关系 | 拓扑排序 |
| 连通区域 | DFS / BFS / 并查集 |

## 五、常见易错点总结

1. 数组下标越界  
   尤其是双指针、二分、矩阵 DFS。

2. 哈希表初始化错误  
   前缀和问题中 `{0: 1}` 经常被漏掉。

3. 递归终止条件不清楚  
   树和回溯题最容易出错。

4. 回溯忘记撤销选择  
   导致路径污染。

5. 动态规划定义模糊  
   `dp[i]` 没定义清楚就写转移，容易全错。

6. 链表断链  
   修改指针前没有保存 `next`。

7. 滑动窗口收缩条件错误  
   分不清“满足时收缩”还是“不满足时收缩”。

8. 去重逻辑错误  
   三数之和、排列组合题非常常见。

9. BFS 层数计算错误  
   最短路径和腐烂橘子这类题要按层推进。

10. 没有考虑空输入  
   面试中很容易被边界用例卡住。

## 六、建议的刷题顺序

可以按题型刷，而不是按题号刷。

推荐顺序：

1. 数组与哈希
2. 双指针
3. 滑动窗口
4. 链表
5. 栈与队列
6. 二叉树
7. 回溯
8. 动态规划
9. 堆
10. 图论
11. 贪心与高级结构

这样刷的好处是，同类题之间可以相互强化，更容易形成模板意识。

## 七、复盘方法

每做完一道题，建议记录：

```text
题目名称：
题型：
核心思路：
关键数据结构：
时间复杂度：
空间复杂度：
易错点：
是否需要二刷：
```

复盘时重点问自己：

1. 我为什么想到这个方法？
2. 有没有更优解？
3. 这题属于哪个模板？
4. 下次看到什么关键词能想到它？
5. 我卡在哪里，是思路问题还是代码细节问题？

## 八、面试表达方式

面试时不要一上来直接写代码，可以按这个顺序说：

1. 先确认题意和边界条件
2. 讲一个直观暴力解法
3. 分析暴力解法复杂度
4. 说明如何优化
5. 解释核心数据结构或状态定义
6. 写代码
7. 说明复杂度
8. 简单跑一个样例

表达示例：

```text
这道题如果暴力做，需要枚举所有组合，复杂度是 O(n²)。
但我们可以用哈希表记录已经遍历过的数字。
遍历当前数字 x 时，只需要判断 target - x 是否出现过。
这样时间复杂度可以优化到 O(n)，空间复杂度是 O(n)。
```

## 九、刷 Hot100 的最终目标

刷 Hot100 不应该只是追求 AC 数量，而是要达到以下状态：

- 看到题目能快速判断大类
- 能说出常见解法和复杂度
- 能写出稳定模板
- 能处理边界条件
- 能复盘出同类题规律
- 面试时能清楚表达思路

真正掌握 Hot100 后，很多中等题都会变成“识别模板 + 调整细节”。这才是刷题最重要的收益。

### 题目描述
给定一个整数数组 `nums` 和一个目标值 `target`，请你从数组中找出 **和为目标值的那两个整数**，并返回它们的数组下标。
你可以假设每种输入只会对应一个答案。但是，数组中同一个元素在答案里不能重复出现。

---

### 解题思路
#### 1) 暴力枚举（直观方法）
- 使用双重循环枚举所有可能的两数组合，检查它们的和是否等于 `target`。
- 这是最直观的方法，但时间复杂度较高，不适合大数据量。

#### 2) 哈希表（一次遍历 / 空间换时间）
这是常用且高效的解法：
1. 遍历数组 `nums`，对于当前元素 `nums[i]`，计算与目标值差值 `need = target - nums[i]`。
2. 在哈希表 `dic` 中检查是否已存在 `need`。
   - 如果存在，说明我们之前遍历过的某个元素与当前元素之和为 `target`，可以直接返回它们的下标。
   - 如果不存在，则将当前元素 `nums[i]` 以及它的下标 `i` 存入哈希表。
3. 由于题目保证有且只有一个答案，因此找到即可返回。

该方法只需一次遍历即可完成，平均时间复杂度为 O(n)。

---

### 复杂度分析
- 时间复杂度：
  - 暴力写法：O(n²)
  - 哈希表（一次遍历）：O(n)
- 空间复杂度：
  - 暴力写法：O(1)
  - 哈希表：O(n)（用于存储已访问的元素及其索引）

---

### 代码（C++）
```c++
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_map<int,int> dic; // 数值 -> 下标
        for (int i = 0; i < (int)nums.size(); i++) {
            int need = target - nums[i];
            auto it = dic.find(need);
            if (it != dic.end()) {
                // 找到之前出现的需要的数，直接返回两个下标
                return {it->second, i};
            }
            // 记录当前数及其下标，供后续查找
            dic[nums[i]] = i;
        }
        return {};
    }
};
```